const config = {
                database: {
                    url: 'mongodb://localhost:27017',
                    dbname: 'node-new',
                    collection: 'users'
                }
            };

            module.exports = config;
